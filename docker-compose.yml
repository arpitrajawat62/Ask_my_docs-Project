version: "3.9"

services:

# Redis
  redis:
    image: redis:7
    container_name: askmydocs_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

